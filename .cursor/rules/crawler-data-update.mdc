---
description: 用户说「更新」时在本机跑爬虫并 push 爬虫 JSON 到 GitHub
alwaysApply: true
---

# 触发词：更新

当用户消息**仅为「更新」**，或明确表达「按项目约定执行数据更新 / 爬虫更新并上传 GitHub」时：

1. **立即**在仓库根目录用终端执行（不要只口述步骤）：
   - `./更新`
   - 若不可执行：`chmod +x 更新 scripts/crawler-update-and-push.sh` 后重试，或 `bash scripts/crawler-update-and-push.sh`
2. 用户附带赛季等参数时，追加传给脚本，例如：`./更新 --season 2025-26`
3. 脚本行为：维护 `爬虫/.venv`、运行 `nba_player_crawler.py`、尽力运行 `scripts/rebuild_front_assets.py`、`git add` 相关 JSON、无变更则跳过 commit，否则 `commit` + `push`。
4. **若失败**（如 BR 403、git 未登录）：如实汇报日志要点，不要编造已推送。
5. **若成功**：说明是否已 `git push`、或「仓库无变化已跳过提交」。
6. 用户未触发「更新」本条规则时，**不要**自动跑上述脚本。
