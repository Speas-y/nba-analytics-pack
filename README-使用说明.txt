NBA 数据分析 — 目录说明
========================
前端/     静态 NBA 数据站（python3 -m http.server 8766）
后端/     Nest API（npm install && npm run start:dev）
爬虫/     Python 爬虫 + output 数据（venv + pip install -r requirements.txt）

未包含：后端 node_modules、后端 dist、爬虫 .venv（需本地重新安装）。
未包含：后端 .env（请复制 .env.example）。
