# Render 手动 Web Service：数据源由 RenderDatabaseConfig 从环境变量生成（见该类注释）
# 仅需在此配置 JPA 方言；勿与本地 application.yml 的 MySQL 混用同一进程
spring:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
